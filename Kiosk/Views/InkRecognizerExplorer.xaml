<Page
    x:Class="IntelligentKioskSample.Views.InkRecognizerExplorer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntelligentKioskSample.Views"
    xmlns:ctl="using:IntelligentKioskSample.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="mainPage">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <CommandBar Style="{StaticResource PageTitleCommandBarStyle}">
                <CommandBar.Content>
                    <TextBlock Text="Ink Recognizer Explorer" Style="{ThemeResource PageTitleTextBlockStyle}"/>
                </CommandBar.Content>
            </CommandBar>
        </StackPanel>
        <RelativePanel Margin="50,125,-45,125" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border RequestedTheme="Light" Background="White" BorderBrush="Black" Margin="0,-55,0,55" Width="82" Height="40">
                <InkToolbar TargetInkCanvas="{x:Bind Path=inkCanvas}" InitialControls="None" Margin="0,0,2,0">
                    <InkToolbarBallpointPenButton/>
                    <InkToolbarEraserButton/>
                </InkToolbar>
            </Border>
            <Grid Background="{ThemeResource SystemControlBackgroundChromeWhiteBrush}" HorizontalAlignment="Left" Height="500" VerticalAlignment="Center" Width="700">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <InkCanvas x:Name="inkCanvas"/>
            </Grid>
            <TextBox x:Name="requestJson" TextWrapping="Wrap" AcceptsReturn="True" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,515,-700,-590"/>
            <TextBox x:Name="responseJson" TextWrapping="Wrap" AcceptsReturn="True" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="750,0,-1225,-75"/>
        </RelativePanel>
    </Grid>
</Page>
